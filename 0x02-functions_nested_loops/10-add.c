#include "main.h"
/**
 * addNum - adds two intergers and returns the results
 *@a:int to be assigned to a
 *@a:int to be assigned to b
 * Return: sum of a and b
 */

int add(int a,int b)
{	int results;
	results= a+b;
   	return(results);
}

